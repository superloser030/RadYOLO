% ============================================================
%  RadYOLO 중간보� 서
%  컴파일: XeLaTeX (Overleaf 설� �: Compiler → XeLaTeX)
% ============================================================
\documentclass[a4paper,12pt]{article}

\usepackage{kotex}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{array}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{tabularx}
\usepackage{multirow}

\geometry{a4paper, top=2.5cm, bottom=2.5cm, left=3cm, right=2.5cm}
\setlength{\headheight}{14.5pt}

% ── 헤더/푸터 ──────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\rhead{RadYOLO 중간보� 서}
\lhead{\leftmark}
\rfoot{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ── 코드 스타일 ────────────────────────────────────────────
\definecolor{codebg}{RGB}{248,248,248}
\definecolor{codeframe}{RGB}{200,200,200}
\definecolor{keyword}{RGB}{0,0,180}
\definecolor{comment}{RGB}{60,128,49}
\definecolor{string}{RGB}{163,21,21}

\lstdefinestyle{python}{
  language=Python,
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  frame=single,
  rulecolor=\color{codeframe},
  backgroundcolor=\color{codebg},
  numbers=left,
  numberstyle=\tiny\color{gray},
  keywordstyle=\color{keyword}\bfseries,
  commentstyle=\color{comment},
  stringstyle=\color{string},
  showstringspaces=false,
  tabsize=4,
}

\lstdefinestyle{plain}{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  frame=single,
  rulecolor=\color{codeframe},
  backgroundcolor=\color{codebg},
  numbers=none,
  showstringspaces=false,
}

% ── � �목 스타일 ────────────────────────────────────────────
\titleformat{\section}{\large\bfseries}{}{0em}{\thesection\quad}[\vspace{0.5ex}\hrule\vspace{0.5ex}]
\titleformat{\subsection}{\normalsize\bfseries}{}{0em}{\thesubsection\quad}

% ── 하이퍼링크 ─────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=blue!70!black,
  urlcolor=blue!70!black,
  citecolor=blue!70!black,
}

% ============================================================
\begin{document}

% ── 표지 ───────────────────────────────────────────────────
\begin{titlepage}
  \centering
  \vspace*{3cm}
  {\LARGE \textbf{RadYOLO 중간보� 서}} \\[1em]
  {\large mmWave � �이더 + 웹�  기반\\실내 3D 씬 재구성 및 실시간 객체 추� �} \\[3em]
  \hrule
  \vspace{1em}
  \begin{tabular}{ll}
    \textbf{GitHub} & \url{https://github.com/superloser030/RadYOLO} \\[0.5em]
    \textbf{작성일} & 2026년 6월 26일 \\
  \end{tabular}
  \vspace{1em}
  \hrule
  \vfill
\end{titlepage}

\tableofcontents
\newpage

% ============================================================
\section{연구 목표}

본 연구는 TI AWR1642BOOST mmWave � �이더와 웹� 을 융합하여
실내 환경의 3D 씬을 자동으로 재구성하� ,
실시간으로 다중 인물을 감지·추� �하는 시스템을 개발하는 것을 목표로 한다.

\subsection{세부 목표}

\begin{enumerate}[leftmargin=2em]
  \item 연구실(송� ) → 기숙사(수� ) 실시간 � �이더/웹�  데이터 � �송 파이프라인 구축
  \item Depth Anything V3(DA3) + � �이더 거리 측� �을 융합한 metric 깊이 추� �
  \item Trellis 3D 모델 생성 및 GigaPose 포즈 추� �을 통한 객체 3D 배치
  \item Three.js 기반 실시간 3D 뷰어에서 배경 포인트 클라우드 및 객체 메시 표시
\end{enumerate}

% ============================================================
\section{시스템 구성}

\subsection{하드웨어}

\begin{table}[h]
\centering
\begin{tabularx}{\textwidth}{llX}
  \toprule
  \textbf{구성 요소} & \textbf{모델} & \textbf{역� } \\
  \midrule
  � �이더 & TI AWR1642BOOST & mmWave IQ 데이터 수집 (TX×2, RX×4) \\
  캡처 보드 & TI DCA1000EVM & LVDS → Ethernet UDP 변환, raw � �장 \\
  카메라 & USB 웹�  & RGB 영상 캡처 (1920×1080, 10fps) \\
  송�  PC & 연구실 노트북 & DCA1000 � �어 + 데이터 � �송 \\
  수�  PC & 기숙사 데스크톱 & 처리, 3D 재구성, 뷰어 실행 \\
  \bottomrule
\end{tabularx}
\caption{하드웨어 구성}
\end{table}

\subsection{네트워크 구성}

\begin{lstlisting}[style=plain]
AWR1642BOOST
  |-- UART (COM3, 115200 baud) --> send_config.py (.cfg � �송)
  |-- LVDS (60pin Samtec) --> DCA1000EVM
                                |-- Ethernet UDP (192.168.33.30:4098)
                                    --> 연구실 노트북 (fordev-kjh, 100.80.189.85)
                                         |-- Tailscale VPN / UDP
                                             --> 기숙사 데스크톱 (forlife-kjh, 100.126.220.19)
                                                  RADAR_PORT = 5006
                                                  WEBCAM_PORT = 5007
\end{lstlisting}

\noindent 대역폭: 핫스팟 환경 평�  약 24.9 Mbps (Tailscale 기준)

\subsection{소프트웨어 스택}

\begin{table}[h]
\centering
\begin{tabularx}{\textwidth}{llX}
  \toprule
  \textbf{구성 요소} & \textbf{버� �/모델} & \textbf{용도} \\
  \midrule
  Python & 3.11 (conda) & � �체 파이프라인 \\
  MATLAB & R2026a & CFAR � �이더 � 호처리 \\
  ComfyUI & - & DA3 깊이 추� �, ESRGAN 업스케일 \\
  Depth Anything V3 & da3\_large & 단안 깊이 추� � \\
  YOLOv8x-seg & Ultralytics & 객체 감지 및 세그멘테이션 \\
  Trellis & fal.ai API & 단일 이미지 → 3D 메시(GLB) 생성 \\
  GigaPose & - & 3D 객체 6DoF 포즈 추� � \\
  Three.js & 0.160.0 & 실시간 3D 웹 뷰어 \\
  \bottomrule
\end{tabularx}
\caption{소프트웨어 스택}
\end{table}

\subsection{디� �� 리 구조}

\begin{lstlisting}[style=plain]
RadYOLO/
|-- main.py                        # � �체 파이프라인 진입� �
|-- config/
|   |-- camera.json                # 카메라 내부 파라미터 (fx, fy, cx, cy)
|   |-- sender.json                # 송�  설� � (fps, quality, � �이더 주기)
|   `-- receiver.json              # 수�  설� �
|-- src/
|   |-- transmission/
|   |   |-- sender.py              # 연구실: 웹� +� �이더 UDP 송� 
|   |   `-- receiver.py            # 기숙사: 수�  + binary � �장 + 타임스탬프
|   |-- background/
|   |   |-- bg_select.py           # 최� � 배경 프� �임 � 택
|   |   |-- upscale.py             # ESRGAN x4 업스케일
|   |   |-- depth.py               # ComfyUI DA3 깊이 추� �
|   |   |-- depth_calibration.py   # � �이더 기반 metric 보� �
|   |   `-- yolo_mask.py           # YOLOv8 세그멘테이션 마스크
|   |-- objects/
|   |   |-- obj_crop.py            # 객체 크롭
|   |   |-- trellis_gen.py         # Trellis 3D 모델 생성
|   |   |-- pose_estimator.py      # GigaPose 포즈 추� �
|   |   `-- flux_kontext.py        # Flux 인페인팅
|   |-- utils/archive.py           # 세션 데이터 아카이브
|   `-- viewer/viewer.html         # Three.js 실시간 3D 뷰어
|-- matlab/
|   |-- cfar_detect.m              # CFAR 검출 -> targets.json
|   `-- process_radar.m
`-- workflows/                     # ComfyUI JSON 워크플로우
    |-- da3_depth.json
    |-- upscale_esrgan.json
    `-- flux_kontext.json
\end{lstlisting}

% ============================================================
\section{� �체 파이프라인}

\subsection{실시간 � �송 (sender.py)}

연구실 노트북에서 실행되며, � �이더 IQ 데이터와 웹�  영상을 동시에 � �송한다.

\begin{enumerate}[leftmargin=2em]
  \item DCA1000EVM CLI로 FPGA 초기화 및 녹화 시작
  \item \texttt{send\_config.py}: pyserial로 COM3에 \texttt{.cfg} 파일 � �송 (\texttt{sensorStart} 포함)
  \item \texttt{radar\_forward()}: DCA1000에서 수� 한 UDP 패킷을 기숙사로 포워딩
  \item \texttt{webcam\_send()}: OpenCV 웹�  프� �임을 JPEG 압축 후 청크 단위로 � �송
  \item DCA1000은 약 70,000 패킷 후 자동 종료되므로, 해당 시� �에 자동 재시작
\end{enumerate}

\subsection{수�  및 � �장 (receiver.py)}

\begin{enumerate}[leftmargin=2em]
  \item � �이더 패킷 수�  → \texttt{data/radar/iqData\_*.bin} � �장
  \item 각 프� �임마다 \texttt{iqData\_timestamps.csv}에 \texttt{frame\_idx, ts\_ms} 기록
  \item 타임스탬프: ms-since-midnight 방식으로 � �이더/웹�  동기화
  \item 웹�  프� �임을 \texttt{frame\_queue}에 � �재 → \texttt{bg\_select.py}에서 소비
\end{enumerate}

\subsection{배경 처리 파이프라인}

\begin{lstlisting}[style=plain]
Step 1  bg_select.py
        10초간 웹�  프� �임 수� , 가장 � 명한 프� �임 � 택
        --> background_raw.jpg, background_ts.txt (배경 촬영 타임스탬프)

Step 2  upscale.py
        ESRGAN x4 업스케일 (ComfyUI 워크플로우)
        --> background.jpg

Step 3  depth.py
        DA3 Large 모델로 단안 깊이 추� �
        � �규화: Standard (near=bright, 밝을수록 가까운 거리)
        --> depth.png

Step 3.5  depth_calibration.py
          � �이더 targets.json에서 타임스탬프 매칭 (±1000ms)
          2-anchor log-linear 모델 fitting
          --> depth.png (보� �됨), depth_calib.json

Step 4  yolo_mask.py
        YOLOv8x-seg로 사람 영역 마스킹
        --> mask.png

Step 5  obj_crop.py
        감지된 객체별 크롭 이미지 � �장
        --> data/objects/<cls>/cutout.jpg

Step 5.3  trellis_gen.py
          fal.ai Trellis API로 단일 이미지 -> 3D 메시 생성
          --> model_trellis.glb

Step 5.5  pose_estimator.py
          GigaPose로 6DoF 포즈 추� �
          --> pose.json, manifest.json

Live    live_update_loop()
        YOLO 위치 갱�  (0.3초), GigaPose 회� � 갱�  (연속)
        --> live.json, pose.json (실시간 갱� )
\end{lstlisting}

\subsection{MATLAB � �이더 � 호처리 (cfar\_detect.m)}

MATLAB에서 별도 실행하며, receiver.py가 � �장한 IQ 데이터를 처리한다.

\begin{enumerate}[leftmargin=2em]
  \item \texttt{iqData\_RecordingParameters.mat}에서 � �이더 파라미터 로드
  \item TX1 홀수 chirp만 사용, 4개 RX에 대해 Range-Doppler 맵 계산
  \item \texttt{phased.CFARDetector2D}로 2D CFAR 검출
  \item 4-element steering vector FFT로 방위각 추� �
  \item 결과 � �장: \texttt{targets.json}
\end{enumerate}

\begin{lstlisting}[style=plain]
targets.json 포맷:
[
  {
    "frame_idx": 0,
    "ts_ms": 12345678,
    "targets": [
      {"range_m": 2.5, "velocity_mps": 0.3, "azimuth_deg": 15.2},
      ...
    ]
  },
  ...
]
\end{lstlisting}

\subsection{DA3 Depth Metric 보� � (depth\_calibration.py)}

DA3가 출� �하는 깊이 맵은 상대� � 깊이(0\textasciitilde1 � �규화)만 � �공하므로,
� �이더 거리 측� �값을 이용해 실� � 미터 단위로 보� �한다.

\subsubsection{보� � 모델}

� �이더 range 값 $R$과 DA3 픽셀값 $D \in [0,1]$ 사이의 관계를
로그-� 형 모델로 근사한다:

\begin{equation}
  \log R = a \cdot D + b \quad \Longleftrightarrow \quad R = e^{aD + b}
\end{equation}

\subsubsection{2-앵커 직� � 풀이}

\begin{itemize}[leftmargin=2em]
  \item 앵커 1: $D_{99\%}$ (가장 밝은 픽셀, near) $\leftrightarrow$ $R_{1\%}$ (가장 가까운 � �이더 타겟)
  \item 앵커 2: $D_{1\%}$ (가장 어두운 픽셀, far) $\leftrightarrow$ $R_{99\%}$ (가장 먼 � �이더 타겟)
\end{itemize}

두 조건으로 2×2 연립방� �식을 구성하여 $a$, $b$를 직� � 풀이한다.

\subsubsection{최근 보� � 결과}

\begin{table}[h]
\centering
\begin{tabular}{lcc}
  \toprule
  \textbf{파라미터} & \textbf{값} & \textbf{의미} \\
  \midrule
  $a$ & $-2.301$ & 기울기 \\
  $b$ & $2.350$ & � �편 \\
  $D=1$ (근거리) & $1.05$ m & 최근� � 추� � 거리 \\
  $D=0$ (원거리) & $10.49$ m & 최원 추� � 거리 \\
  \bottomrule
\end{tabular}
\caption{depth\_calibration.py 보� � 결과 (2026-06-26)}
\end{table}

보� �된 깊이 맵은 log-scale 재� �규화를 � �용하여 근거리와 원거리의 시각� � 비중을 � 등하게 만� 다:

\begin{equation}
  D_{\text{norm}} = 1 - \frac{\ln R - \ln R_{\min}}{\ln R_{\max} - \ln R_{\min}}
\end{equation}

\subsection{3D 뷰어 (viewer.html)}

Three.js 기반 웹 뷰어로, \texttt{python main.py --viewer-only} 실행 시
\texttt{http://localhost:8000/src/viewer/viewer.html}에서 � �속한다.

\subsubsection{포인트 클라우드 투영}

깊이 맵 픽셀 $(p_x, p_y, d)$를 3D 공간으로 투영�  때
올바른 원근 투영(perspective projection)을 � �용한다:

\begin{align}
  d_{\text{cam}} &= e^{a \cdot d + b} \quad \text{(depth\_calib.json 로드 시)} \\
  X &= \frac{p_x - c_x}{f_x} \cdot d_{\text{cam}} \\
  Y &= -\frac{p_y - c_y}{f_y} \cdot d_{\text{cam}} \\
  Z &= -d_{\text{cam}}
\end{align}

$X$, $Y$에 깊이를 곱하지 않으면 바닥·벽이 올바른 평면으로 재구성되지 않는다.

\subsubsection{객체 오버� �이}

GigaPose \texttt{pose.json}에서 회� � 행� � $R$과 이동 벡터 $t$를 읽어
카메라 좌표계 → Three.js 좌표계로 변환 후 GLB 메시를 씬에 배치한다.

\begin{equation}
  F = \mathrm{diag}(1, -1, -1), \quad R_{\text{view}} = F R_{\text{cam}} F^T
\end{equation}

% ============================================================
\section{주요 트러블슈팅}

\subsection{DCA1000 자동 종료 문� �}

\textbf{증상}: DCA1000EVM이 약 7,000 패킷 수�  후 자동으로 녹화를 종료함.

\textbf{원인}: DCA1000 하드웨어의 내부 버퍼 한계로 인해 장시간 연속 녹화가 불가능.

\textbf{해결}: \texttt{sender.py}에서 시퀀스 번호가 \texttt{RESTART\_AT\_SEQ = 70000}에 도달하면
\texttt{stop\_record} → \texttt{start\_record}를 자동 실행하여 세션을 재시작.
재시작 중 수집되지 않는 프� �임은 타임스탬프 기반으로 식별 가능.

% ============================================================
\section{현재 구현 상태}

\begin{table}[h]
\centering
\begin{tabularx}{\textwidth}{lXl}
  \toprule
  \textbf{기능} & \textbf{내용} & \textbf{상태} \\
  \midrule
  실시간 � �송 & � �이더/웹�  UDP � �송, 타임스탬프 동기화 & \textcolor{green!60!black}{완료} \\
  배경 � 택 & 10초 수집 후 최� � 프� �임 � 택 & \textcolor{green!60!black}{완료} \\
  ESRGAN 업스케일 & 배경 이미지 해상도 향상 & \textcolor{green!60!black}{완료} \\
  DA3 깊이 추� � & 단안 깊이 맵 생성 & \textcolor{green!60!black}{완료} \\
  Depth Metric 보� � & � �이더 기반 log-linear 보� � & \textcolor{green!60!black}{완료} \\
  CFAR � �이더 검출 & MATLAB, targets.json 출� � & \textcolor{green!60!black}{완료} \\
  YOLO 마스킹 & 사람 영역 세그멘테이션 & \textcolor{green!60!black}{완료} \\
  Trellis 3D 생성 & 단일 이미지 → GLB 메시 & \textcolor{green!60!black}{완료} \\
  GigaPose 포즈 추� � & 6DoF 포즈 추� � & \textcolor{green!60!black}{완료} \\
  3D 뷰어 & 포인트 클라우드 + GLB 오버� �이 & \textcolor{green!60!black}{완료} \\
  \bottomrule
\end{tabularx}
\caption{구현 현황}
\end{table}

% ============================================================
\section{향후 계획}

\subsection{DA3 Depth 보� � � 도화}

현재 보� �은 � �이더 range 분포와 DA3 픽셀 분포를 2개의 앵커 포인트로 매칭하는
단순 log-linear 모델을 사용한다.
향후 � �이더 방위각 � �보와 카메라 투영을 결합하여 픽셀 단위의 � �밀 보� �을 구현하� ,
보� � � 뢰도를 � �량� �으로 평가�  예� �이다.

\subsection{실시간 객체 위치 뷰어 표시}

YOLO로 감지된 객체의 2D 바운딩박스 중심을 깊이 맵으로 역투영하여
3D 공간 좌표를 계산하� , 뷰어에 실시간으로 표시한다.
� �이더 타겟과 YOLO 바운딩박스를 매칭하여 ID 기반 다중 인물 추� �을 구현�  예� �이다.

\subsection{사람 3D 메시 실시간 � �더링}

감지된 사람 영역을 Trellis 또는 별도 인체 메시 모델을 이용해
3D 메시로 생성하�  뷰어에 실시간으로 � �더링한다.
포즈 추� � 결과를 반영하여 방향까지 표현하는 것을 목표로 한다.

\subsection{동� � 배경 자동 갱� }

카메라 시야 내에 이� �에 보이지 않았던 배경 영역이 새롭게 노출되면,
해당 영역을 자동으로 감지하�  Flux 인페인팅으로 배경 맵을 갱� 한다.
사람이 이동하면서 가� �졌던 벽이나 바닥이 드러�  때
자연스럽게 3D 씬이 보완되는 효과를 목표로 한다.

\end{document}
